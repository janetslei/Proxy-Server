Proxy Server

Group 16
Janet SLeiman
Caleb Smith
Olaf Meyer

Create a directory called 'cache' in the directory of the executable to store the cache files.

Compile: gcc s.c -lpthread

Run: ./a.out

Connect to the proxy from a web browser using 'http://<server address>:<port>/www.website.com' with port = 1234
